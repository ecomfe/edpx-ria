/**
 * @file 引入公共模板
 * @author {{{author}}}({{{authorEmail}}})
 */

define( 
    function ( require ) {
        // 这里引入公共的模板
        // require( 'er/tpl!./list.tpl.html' );

        return {};
    }
);
